linters:
  auto-fix: false
  enable:
    - nakedret 
    - interfacer
    - varcheck
    - deadcode 
    - structcheck 
    - misspell
    - maligned 
    - ineffassign
    - goconst
    - goimports
    - errcheck 
    - dupl 
    - unparam 
    - golint 
    - staticcheck
    - unused
    - gosimple
    # missing to be added
    # - lll (250), gocyclo, gosec, scopelint, unconvert, stylecheck
run:
linters-settings:
  errcheck:
    check-type-assertions: true
