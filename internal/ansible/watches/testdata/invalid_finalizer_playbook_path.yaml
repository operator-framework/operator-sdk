---
- version: v1alpha1
  group: app.example.com
  kind: Database
  playbook: playbook.yaml
  finalizer:
    name: app.example.com/finalizer
    playbook: playbook.yaml
    vars:
      sentinel: finalizer_running
