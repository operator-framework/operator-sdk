---
- version: v1alpha1
  group: app.example.com
  kind: Database
  playbook: playbook.yaml
  finalizer:
    name: finalizer.app.example.com
    role: ansible/role
    vars:
      sentinel: finalizer_running
