# entries is a list of entries to include in
# release notes and/or the migration guide
entries:
  - description: >
      For Ansible-based and Helm-based operators, the Docker base image is
      pinned to improve the reproducibility of builds using Docker.

    kind: addition

    # Is this a breaking change?
    breaking: false
