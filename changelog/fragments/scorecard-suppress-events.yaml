entries:
  - description: >
      Updated the sample kuttl-test.yaml scorecard configuration
      file to specify 'suppressLog: events' which disables kuttl
      from logging k8s events and allows for RBAC priviledge descalation.

    kind: "change"

    breaking: false
