# entries is a list of entries to include in
# release notes and/or the migration guide
entries:
  - description: >
      Removed `pkg/k8sutils` and its helpers methods `GetWatchNamespace()`, 
      `GetOperatorNamespace()`, `GetOperatorName()`, `ResourceExists()`, `GetPod()`,
      `GetGVKsFromAddToScheme()` and `SupportsOwnerReference()`. 

    kind: "removal"

    breaking: true

    migration:
      header: Removed k8sutils
      body: >
        TBD
