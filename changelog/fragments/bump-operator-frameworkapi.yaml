# entries is a list of entries to include in
# release notes and/or the migration guide
entries:
  - description: >
      Optional `OperatorHub.io` validator called by `operator-sdk bundle validate ./bundle --select-optional name=operatorhub` now includes a previously missing category `Modernization & Migration`.
    kind: "bugfix"
    breaking: false
  - description: >
      Improves the validate bundle spec error message top rovide a better explanation when invalid service accounts are found.
    kind: "bugfix"
    breaking: false