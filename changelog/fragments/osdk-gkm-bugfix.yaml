# entries is a list of entries to include in
# release notes and/or the migration guide
entries:
  - description: >
      `operator-sdk generate kustomize manifests`: update to handle generating manifests for complex types
    kind: "bugfix"
    breaking: false

