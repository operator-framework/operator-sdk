entries:
  - description: >
      Renamed `--max-workers` flag to `--max-concurrent-reconciles`. Added
      `MAX_CONCURRENT_RECONCILES_<Kind>_<Group>` environment variable to Ansible-based
      operators. Also includes a deprecation message for `--max-workers` flag and
      `WORKERS_<Kind>_<Group>` environment variable.
    kind: "addition"
